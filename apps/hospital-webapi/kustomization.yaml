apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- 'https://github.com/FilipPauco/hospital-webapi//deployments/kustomize/install?ref=v1.0.0'
- openapi-ui.service.yaml
- http-route.yaml

labels:
- pairs:
    wac-fiit.github.io/component: backend

patches:
- path: patches/hospital-webapi.deployment.yaml
